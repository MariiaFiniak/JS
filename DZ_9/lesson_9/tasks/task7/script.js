"use strict"
// Дано масив елементів.
//  Усі елементи, які більші за перший елемент помножити на 2

// Дано масив цін. 
// Змінити цей масив так, що на ціни товарів, які більші за 1000 грн.дати 30 % знижки.

if (confirm("Почати тестування?")) {
  
	let product = [860, 580, 1240, 3900, 4500, 234]
	product.forEach((el, index, arr) =>{
		if (el>1000) arr[index]*=0,3
	})
	document.write(`${product}`)
}
